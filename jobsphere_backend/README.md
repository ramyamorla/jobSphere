# jobsphere_backend

## Run

Prerequisites:

- Java 17
- Maven 3.9+
- MongoDB running locally (or set `MONGODB_URI`)

Commands:

```bash
mvn spring-boot:run
```

Health check:

```bash
curl http://localhost:8080/api/health
```
